# Official S00A engineering workspace

This fresh clone starts at the verified clean main baseline. The original E workspace remains local and is not this repository. Run `python tools/mosei_flow.py doctor`, `python -m unittest discover -s tests`, and `python tools/mosei_flow.py gate --run-id <id>`. See `TASK_SPEC.md` and `reports/bootstrap/MIGRATION_NOTES.md`. No research data audit or model experiment is authorized in S00A.
