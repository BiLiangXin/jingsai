# S04-CLARIFY-02 — clarified preprocessing and mapping

Authority: the current user's S04_CLARIFIED_PREPROCESS_AND_MAPPING request. Model setting is user requested GPT-6 Sol / High; runtime telemetry is not available. The previous S01/S02/S03/S04 authorizations, failures, claims, model results and frozen artifacts remain historical. New predictor training fits: zero. Formal special output covers aligned only. The original M2 seed17 epoch2, TRAIN scaler and V1 remain fixed.

Three newly reported contest clarifications change only this task's permissible preprocessing and mapping: aligned-only final special output; Attachment3 text feature extraction as preprocessing; and evidence-backed reproducible estimated source mapping without requiring an official timing sidecar. These do not establish that any particular encoder recreates the old 50×768 representation, nor do they make estimated timestamps official truth.

Branch A first audits numerical semantics of all three `text_bert` channels without relying on dtype alone. Any proposed encoder must have identified tokenizer, vocabulary, checkpoint, layer, positional, padding and missingness rules. TRAIN/VALID replay must compare generated continuous text and frozen M2 outputs to the original interface before a freeze or Attachment3 inference. No ID-based clean reconstruction, TEST use, special-data fitting, invented vector, or silent dtype coercion. If equivalence fails, specify `NEW_PIPELINE_REQUIRED` using one common encoder over TRAIN/VALID/Attachment3; a new M2-E1 would require future authorization and would inherit no old M2 metrics.

Branch B preserves the previous 20-row private predictions and feature explanations byte for byte. A feature-to-token/text-to-acoustic-time-to-video-PTS chain may be estimated if its actual sources, algorithm, validation and error are recorded. Special tokens, padding and unmatched positions receive separate null/quality statuses. No index/50×duration, uniform word times, official-truth claim or AI human sign-off. Existing Q1 counts remain 100 extracted, 66 programmatic passes, zero human verified. Attachment3 and Attachment4 decisions are independent.

All actual tests, an independent read-only technical review of the exact candidate, tracked/index/worktree safety scan and normal exact-path commit/push precede any new final special inference. No training, additional model search, TEST, special truth, fitted special statistics, Release or contest submission. The old S04 private CSV/ZIP and public receipts are not overwritten.
