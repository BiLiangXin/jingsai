# S04-DEPLOY-03: E1_VISIBLE_UNK before execution

Authority: current user/main-research decision. New 45-minute window began 2026-09-25 09:27:40 UTC and ends 10:12:40 UTC, earlier than the submission-buffer deadline. No predictor/encoder fit, model selection, threshold selection, TEST or special truth. The old S04 tasks and claims remain historical. Frozen D-DATA-01..07, M2 seed17 epoch2, TRAIN scaler and V1 are unchanged.

The single policy is `E1_VISIBLE_UNK_M2_V1`: validate all three stored BERT channels as finite integral values; keep legal IDs, attention mask and segment IDs unchanged (including ID100); run the pinned eval `BertModel.last_hidden_state`; feed the resulting continuous text, original audio/vision and channel-1 support to the unchanged V1 adapter and M2. Never infer the unknown token's original lexical identity, change the mask from ID100, set the token to PAD/MASK/zero, or use a token as an additional sentiment predictor input. The encoder is a fixed general pretrained feature extractor. This is a deployment policy for received visible tokens, not a claim about the organizer's corruption generator or a transfer of old S02 feature-mask performance.

Precommitted gate: rehash prior 96 clean rows and the first failed strict replay; verify local vocab/tokenizer/weights (100 must be `[UNK]`, distinct from PAD0/MASK103 and label ignore -100). Compare the same E1 function against original continuous text and frozen M2 on **all 728 VALID** rows: active-coordinate `atol=5e-4, rtol=1e-4`, max abs class-logit and regression delta `1e-4`, 728/728 argmax agreement. Retain any failure; do not widen tolerance. Use no label for this interface comparison. On VALID ordinals 8..39, run exactly one T-only `('T','0.3','middle')` R01 window with valid root1103; replace only legal interior token IDs with 100 before E1, preserve support and A/V. If the frozen window touches CLS/SEP or has no eligible interior, record `CLEAN_ONCE` rather than resample. Record all 32 outcomes, including class changes and nonzero/zero deltas, without selecting a model or interpreting this as attempted96.

After synthetic and actual checks, an independent read-only reviewer assesses exact code/dependency manifest and evidence. Safe exact-path commit/push and fresh local=remote/clean-worktree/hash checks precede one new private claim and aligned30 final inference. Original source SHA and size must match the already recorded official structure inventory before/after reading; no unaligned content. Same E1 function handles records with and without ID100. Output 30 rows or a true BLOCKED result, never skipped rows or neutral fallback. Derived embeddings and sample CSV remain private. Special performance remains null/UNKNOWN.

Attachment4 prediction/attribution/windows are immutable. Audit class and regression primary modalities plus **all** fixed key windows by text span, acoustic interval, actual frame/PTS and human verification separately. Text spans may be valid without seconds; audio/vision temporal evidence requires corresponding support. Human checks remain zero unless a team member actually signs. Paper and material listing are synchronized as draft/partial states, with old M2 validation, E1 clean replay, UNK stress diagnostic and unlabeled special inference distinct.
