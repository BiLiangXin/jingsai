# S04-IO-RECOVERY-01 — bounded delivery protocol

Authority: current user S04_IO_COMPATIBILITY_AND_DELIVERY_RECOVERY. One official-tree writer. No training fits, optimizer, parameter/buffer update, model/profile/threshold/seed selection, TEST use, Release, or upload. S01/S02/S03 remain CONSUMED. Absolute compute stop 2026-09-25 18:00 Beijing; this task's 90-minute limit starts 2026-09-25 14:59 Beijing and ends 16:29 Beijing. Private run root is outside the repository. Fresh one-shot claim and output, never old S03 claim.

## Immutable predictor

M2 seed17 epoch2, original best SHA256 f99426be8d90b8abf196faf9cf92a41c5ab607da661a7a91530f1fe481a0ceb9, existing TRAIN z-score scaler, and selected V1. V1 computes support AND current raw visible nonzero for each modality before normalization. This proxy is neither original observed truth nor artificial corruption truth. Ordered classes Negative, Neutral, Positive; fixed regression in [-3,3]. Check model state and scalar normalizer against registered originals before prediction and after. The inference-only export is allowed only after exact tensor/scaler equivalence.

## Attachment3

The prior S03 runtime stopped on KeyError text after opening its first object. This new task may inspect only verified official Attachment3 file structure, not values, labels, IDs or prediction distributions. Accept only evidence-backed lossless organization changes. Continuous text N×50×768 and legal text_bert channel1 support are required; audio/vision N×50×74/35. No token-ID substitution, generation, imputation, resizing or outside recovery. If any required field truly absent or ambiguous, record REQUIRES_OFFICIAL_SCHEMA_CLARIFICATION and stop this branch. An eventual execution would require synthetic and authorized TRAIN/VALID equivalence, independent Sol high read-only technical review, reviewed commit/push, clean matching remote, fresh private claim, then full output checks. Old failures and claims remain.

## Attachment4 gates fixed before inspection

Fixed Q3 model: same M2 seed17/scaler; V1 current visible adapter. Classification target for every coalition is the **full-input predicted class logit**, held fixed; regression target is the scalar strength. Standardized zero is the content reference, while support and availability, including b/rho, remain fixed. Enumerate all 8 T/A/V coalitions and compute exact grouped Shapley contributions per target, signed and absolute-normalized. If all absolute contributions vanish, status NO_RESOLVABLE_EFFECT. Local evidence uses width 3 supported positions, stride 1, at most 3 disjoint windows by absolute deletion effect, deterministic tie by lower feature index; report class and regression separately. These are feature-content interventions, not causal source effects.

Gate A: verified official aligned continuous features and legal support; finite full prediction and exact coalition integrity; unique rows and consistent source membership. Output is a **private PARTIAL** CSV even if mapping cannot be established. Fixed columns: sample_index, source_file, source_row, sample_id, polarity, intensity, class_target, class_phi_T, class_phi_A, class_phi_V, reg_phi_T, reg_phi_A, reg_phi_V, class_main_modality, reg_main_modality, class_effect_status, reg_effect_status, class_relative_T, class_relative_A, class_relative_V, reg_relative_T, reg_relative_A, reg_relative_V, class_windows_json, reg_windows_json, mapping_status. Window JSON retains feature_start, feature_end_exclusive, timestamp_start/end (null if unverified) and signed intervention scores. No label or true outcome.

Gate B: a traceable correspondence from these exact official model feature coordinates to the source MP4 or token/audio/frame timeline. Official sidecar or actual feature-generation metadata must establish index semantics, special tokens, truncation, frame/segment bounds and each source association. Equal lengths/file names alone do not prove it. Verified official coordinates get VERIFIED_MAPPING; validated audio-derived estimates get ESTIMATED_ALIGNMENT with provenance, never official truth. Without sufficient evidence, retain feature indices with null times and UNVERIFIED_MAPPING; Gate B fails and no COMPLETE claim. An A4 Gate B failure never changes Gate A or Attachment3.

All data access remains bounded to verified official local sources. No special labels, summary statistics for tuning, ID matching to other splits, score-based interface changes, or output-dependent interpretation changes. Source manifests and private sample outputs remain outside Git. Public receipts contain counts, hashes, schema and gate status only.
