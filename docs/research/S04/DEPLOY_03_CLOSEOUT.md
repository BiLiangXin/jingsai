# S04-DEPLOY-03 visible-UNK closeout

The present main-research decision authorizes one E1_VISIBLE_UNK_M2_V1 deployment policy, not a new predictor fit. The earlier S04-CLARIFY-02 conclusion and its strict replay failure remain historical evidence. This new policy does not claim to reconstruct missing original words or the organizer's corruption procedure. The fixed local tokenizer maps positive ID 100 to `[UNK]`; PAD=0, CLS=101, SEP=102 and MASK=103, while label ignore `-100` is unrelated. All legal token IDs, stored attention mask and segment IDs pass unchanged to the pinned general BERT `last_hidden_state`; its continuous output feeds the unchanged M2 seed17 epoch2, TRAIN scaler and V1. No MLM fill, original-text recovery, model selection or training occurred.

The previous 96-row clean replay and the initial failed strict tolerance receipt were rehashed. Before special inference, the same E1 function matched all 728 VALID clean rows under the prespecified active-coordinate backend envelope (`atol=5e-4`, `rtol=1e-4`): maximum feature difference `2.8514862060546875e-4`, maximum class-logit difference `4.5299530029296875e-6`, maximum regression difference `3.069639205932617e-6`, classification agreement 728/728. These are interface equivalence checks without VALID labels or a performance-selection claim. The one fixed 32-row T/0.3/middle continuous-UNK diagnostic (VALID ordinals 8..39, root1103) changed 14 class predictions; no clean fallback was needed in these 32 cases. It neither requires invariance nor substitutes for the old attempted96 experiment.

The eight synthetic rejection/preservation tests and three directly affected prior tests passed. Independent read-only technical review checked 12/12 manifest members and the 30/30 official aligned-source hashes/sizes. The 11-file candidate set, tracked index and worktree passed the publication scanner; preinference commit `72463585b78a1f317d4dc5601f812efe7a494095` was pushed and local/remote/clean state checked. Runtime then rechecked hashes, source population, deadline, manifest, review, model and scaler before writing a new one-time claim. All 30 aligned Attachment3 files used exactly the same E1 path, including those without ID100. The private 30-row CSV was read back for population, unique identifiers, allowed polarities and finite `[-3,3]` values; SHA256 `27a62a2a262399a3ecf48e6998fb26703329c0bf2c1f1837ce2f50d225e92adb`. No unaligned file, TEST field or special truth was used. Attachment3 Accuracy, macro-F1, MAE and Pearson remain null/UNKNOWN.

Attachment4's 20 historical predictions/contributions and mapped PARTIAL CSV remain byte-identical (`9f1846d9ad4a1ab9d8a7d968503b87ff5e7f675b9b721855cc7c7eeccfcde3e0`). A separate audit retained both fixed key-window sets: 120/120 have text word-index positions, 39 contain unmapped token positions, and 72 windows in 12 records have estimated acoustic word intervals plus nearest actual video PTS. The other eight records have text spans only. Text spans do not require seconds to be useful; the shared token windows and nearby PTS do **not** prove the official audio/vision feature coordinate to source-time mapping. Classification primary modality is text for 17 and vision for 3; regression primary modality is text for 18, audio for 1 and vision for 1. Primary modality/windows were not changed to evade missing location support. Human review is 0/20 for Attachment4 and 0/100 for Q1; Q1's 66 machine-checked alignments and 34 failures remain separate.

The anonymous paper was minimally synchronized into a new private DOCX draft: new §4.4 distinguishes the E1 replay, UNK diagnostic and unlabeled special prediction; §4.3 reports Attachment4's partial mapping and limitations. ZIP member set, CRC and 94-paragraph parse passed. Its SHA256 is `df159461d24bea20b5b4f792f25a9f1a4b533c09d6539f984daa52fc81ab008d`. The private material index points to the new Attachment3 CSV, unchanged Attachment4 PARTIAL CSV, this paper draft and Q1 assets. It is not a complete anonymous submission package; team paper review, actual media checking and Attachment4 mapping/delivery remain.

Evidence levels: VERIFIED for tokenizer identity, fixed-file hashes, saved full replay/diagnostic, actual tests/review/commit/inference/readback and paper container checks; SPECIFIED for the E1 policy; UNKNOWN for organizer corruption order, unlabeled special performance and audio/vision feature-time correspondence. The 32-row UNK diagnostic is deliberately not a performance result. This task used Python 3.11.14, PyTorch 2.9.1+cu128, Transformers 4.44.2 and CUDA 12.8 on NVIDIA GeForce RTX 5070 Ti. Exact run commands and exit codes are summarized in `reports/s04_deploy_03/TESTS.json`; binding, aggregates and immutable output hashes are in `reports/s04_deploy_03/`. The 45-minute execution window was 2026-09-25 09:27:40–10:12:40 UTC; execution finished before its cap and before the submission buffer. No Release or contest upload occurred.
