
# S05 evidence and package closeout

S05-CLOSEOUT-01 is consumed. **VERIFIED:** frozen M2 seed17 epoch2/TRAIN scaler/V1 and Attachment3 original 30-row aligned CSV were rehashed, read back and source-matched; Attachment3 was not rerun. The original 20 Attachment4 predictions, contributions and windows remain cell-identical. A fixed width-three, one-modality-at-a-time deletion supplement passed 36 locked VALID cases and 2340 candidate-window invariants before one 20-row final supplement. The new explanation is a feature-space intervention on the full-input class logit or regression output, not Shapley or causality. A new mapped private CSV preserves every old/supplement column.

**VERIFIED/SPECIFIED mapping:** 120/120 original key windows have text positions; 39 contain a special/punctuation token position without a direct word span, not 39 wholly unmapped windows. Real-audio forced alignment gives 12/20 records and 72 original windows estimated intervals; 8/20 remain text-only. Nearby true video PTS was measured relative to estimates. The shared aligned index is specified by the problem statement; exact official audio/vision feature-production timestamps remain **UNKNOWN**. Priority review is 18 distinct records: nontext primary union 3, no-acoustic 8, unmapped-token records 14, overlapping. Q1 100 features, 66 mechanical checks, 34 exceptions and 0 human reviews; A4 0/20 human reviews. Private workbench cards and blank export forms are ready for the team; no AI signature was entered.

**Paper/materials:** the registered S04 latest source DOCX was parseable but failed Microsoft Word Open. Its body, two tables and four figures were rebuilt as a new anonymized DOCX and 8-page PDF; each rendered page was visually checked. The paper distinguishes 728/728 clean interface class agreement from correctness, 14/32 UNK sensitivity from accuracy, and unlabeled special outputs from metrics. A private 13,448,656-byte SUBMISSION_CANDIDATE_S05_V5.zip contains 100 Q1 feature files, frozen model/scaler export, code/config/environment, both result CSVs, paper and evidence; 273 members passed fresh-unpack byte SHA256/size, CRC, set and path checks. Identity/path/credential scan found zero hits. The standalone package integrity command passed and verifies the original 30×50×768 E1 text cache byte hash. The prespecified unpacked numerical prediction recomputation gate was **NOT_RUN/DEPENDENCY_REQUIRED**: the bundle has no bound Attachment3 audio/vision tensors, S05 forbids rerunning Attachment3, and the fixed generic BERT weights and official sources are external to the 50MB bundle. A byte-verified text cache alone does not establish predicted-output tolerance. The package is a candidate, not a completed submission.

**Negative results retained:** first VALID36 runner misread an old attribution card as a scalar, then was corrected against the historical coalition tensor; direct Word opening of source/first edited DOCX failed, requiring controlled rebuild; first package manifest missed nested figures/MANIFEST.json and was rejected. No fit, A3 rerun, TEST selection, special truth, Release or contest upload occurred. Real commands/results and private SHA256 are in `reports/s05_closeout/TESTS.json` and `RESULT.json`; the private workbench is outside Git. The old S03/S04 failures and claims remain historical. Next owner is the team for actual Q1/A4 media review and final submission check.
