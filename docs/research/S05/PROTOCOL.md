# S05-CLOSEOUT-01 fixed protocol

Start 2026-09-25 10:27:03 UTC; stop new S05 computation by 11:27:03 UTC or the submission deadline minus 60 minutes, whichever is earlier. Sole official tree writer; zero predictor/encoder fits and no old claim revival. The official 30-row A3 CSV is rehashed, read back and source-bound only; it is never regenerated. The original 20 A4 prediction, contribution and joint-window columns are immutable.

**S05-MAP-01.** An aligned sequence position is shared across T/A/V in the problem statement. For display, map its verified text token/word span to a genuine audio word interval where one exists, then to actual nearby video PTS. Label `ALIGNED_REFERENCE_TIME_ESTIMATE`. Preserve the distinct field `official_av_feature_production_timing=UNKNOWN`. A text span without seconds is still a text span. Do not assign special-token or punctuation times, divide clip duration by 50, or treat nearest PTS as the origin of an audio/vision numerical feature. Existing 12 estimated alignments are reused. The other eight remain text-only unless a single documented minimum correction with source evidence passes; no candidate shopping.

**S05-EXPL-01.** Existing A4 local windows jointly delete T/A/V and remain unchanged. For each modality separately, inspect original supported-coordinate contiguous width-three windows at stride one. In each eligible window replace only that modality's available content with the same TRAIN-normalized zero content reference; preserve other modalities, support and availability masks. Keep the original full-input predicted class logit and regression output as fixed targets. Signed effect is full minus single-window-deleted output. Rank by descending absolute effect, tie by earlier start, choose at most three nonoverlapping windows per modality and target. If none, record `NO_AVAILABLE_CONTENT_EFFECT`. No model parameters/buffers, selection thresholds or original A4 columns change. Synthetic and fixed VALID36 implementation checks precede one A4 pass. These are local model interventions, not causal effects or new performance metrics.

The team review workbench lists all 20 cases with priority as the true union of nontext-primary, absent acoustic estimate, and mapping anomaly groups. Human fields start blank. Reviewers must actually inspect source audio/video before checking them. The final candidate ZIP is private, at most 50 MB across the declared attachment set, with manifest/hash/size/CRC/path/anonymous checks and an unpacked official-output recomputation. Recomputing from saved continuous E1 cache is distinct from rebuilding it from token IDs with external pinned general BERT. Any unverified layer stays explicit. Only safe code/aggregates enter Git; no Release or contest upload.

Closeout observation: the planned unpacked official-output numerical recomputation gate remained unmet. The available prior cache contains only A3 E1 text; S05 did not reread A3 contents or rerun its predictions. This is a reported limitation, not an ex post change to the frozen explanation or mapping protocol.
